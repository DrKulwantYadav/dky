import { NextResponse } from "next/server";import{z}from"zod";import{getAdmin}from"@/lib/admin/auth";import{createClient}from"@/lib/supabase/server";
const schema=z.object({id:z.string().uuid(),confirmation_status:z.enum(["Pending","Confirmed","Rejected"]).optional(),registration_status:z.enum(["New","Contacted","Confirmed","Appointment Booked","Completed","Cancelled","No-show"]).optional()}).refine(x=>Object.keys(x).length>1);
export async function PATCH(req:Request){const admin=await getAdmin();if(!admin)return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid update"},{status:400});const{id,...values}=parsed.data;const s=await createClient();const{error}=await s.from("regular_registrations").update(values).eq("id",id);if(error)return NextResponse.json({error:error.message},{status:400});await s.from("admin_activity_logs").insert({admin_id:admin.user.id,action:"Regular registration updated",registration_id:id,metadata:values});return NextResponse.json({ok:true});}
